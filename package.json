{
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro build && pagefind --site dist --output-subdir pagefind && astro preview",
    "postbuild": "pagefind --site dist --output-subdir pagefind"
  },
  "dependencies": {
    "astro": "^3.0.8"
  },
  "devDependencies": {
    "@astrojs/mdx": "^1.1.0",
    "@astrojs/prism": "^3.0.0",
    "astro-icon": "^0.8.1",
    "autoprefixer": "^10.4.15",
    "bootstrap": "^5.3.1",
    "pagefind": "^1.5.2",
    "sass": "^1.66.1",
    "stylelint": "^15.10.3",
    "stylelint-config-standard-scss": "^11.0.0"
  }
}
